/**
 * Root page — renders the PreQual marketing/prototype shell.
 * The shell is a client component because it owns all navigation state.
 */
import { PreQualShell } from '@/components/PreQualShell'

export default function Home() {
  return <PreQualShell />
}
